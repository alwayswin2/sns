[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=/app/src uvicorn src.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
