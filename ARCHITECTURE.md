# 에어비앤비 정산 자동화 — 시스템 현황 문서

> 작성일: 2026-09-09
> 목적: 현재 구조·비용·장애 이력을 외부 검증용으로 정리. 대안 아키텍처 검토의 입력 자료.

---

## 1. 이 시스템이 하는 일

에어비앤비가 보내는 **정산(지급) 안내 메일**을 Gmail에서 읽어, 게스트별 예약 내역으로
파싱한 뒤 **Notion 데이터베이스(정산 장부)에 한 행씩 기록**한다.

```
에어비앤비 정산 메일 ──→ Gmail
                            │
              (주기적 폴링) │
                            ▼
                    FastAPI 웹서버  ──파싱──→  Notion 정산 장부
                            ▲
                            │
       은행 입금 문자 ──→ MacroDroid ──→ /webhook/sms
```

핵심 성격:
- **1박 이상 숙박이 최소 단위**라 실시간성이 필요 없다. 몇 시간 지연은 무방하다.
- 처리량이 매우 작다. **정산 메일은 하루 0~2건 수준**이고, 누적 대상 메일이 약 50건이다.
- 사용자는 1인(호스트 본인). 동시 사용자, 인증, 확장성 요구가 없다.

---

## 2. 현재 구성 요소

| 역할 | 위치 | 비고 |
|---|---|---|
| 웹서버 (실제 로직 전부) | Railway 프로젝트 `splendid-warmth` / 서비스 `sns` | FastAPI, 상시 가동 |
| 크론 트리거 (현행) | GitHub Actions `.github/workflows/check-emails.yml` | 2026-09-08 이전 |
| 크론 트리거 (구버전, **중단됨**) | Railway 프로젝트 `noble-inspiration` / 서비스 `email-check-cron-v2` | 2026-09-09 스케줄 해제·배포 제거. 서비스 껍데기만 롤백용으로 보존 |
| 데이터 저장소 | Notion 데이터베이스 | 이게 사실상 유일한 DB |
| 문자 트리거 | 안드로이드 MacroDroid → `/webhook/sms` | 보조 경로 |

공개 엔드포인트: `https://sns-production-7b5b.up.railway.app`
- `GET /` — 헬스체크
- `POST /webhook/check-emails` — 메일 확인 트리거 (크론이 호출)
- `POST /webhook/sms` — 입금 문자 수신

인증은 `x-secret` 헤더 고정 문자열 하나뿐이다.

### 코드 규모
전체 **566줄**(Python 7개 파일). 실제 핵심은 3개 파일이다.

| 파일 | 줄 | 역할 |
|---|---|---|
| `src/email_parser.py` | 148 | 메일 본문 정규식 파싱 (**장애 다발 지점**) |
| `src/gmail_watcher.py` | 125 | Gmail 조회 + 처리 루프 |
| `src/notion_db.py` | 96 | Notion 기록/중복 확인 |
| `src/main.py` | 77 | FastAPI 엔드포인트 |
| 기타(셋업/재인증 스크립트) | 120 | 1회성 도구 |

**로직의 규모가 서버 한 대를 상시 띄울 만한 수준이 아니라는 점이 이 문서의 핵심 논점이다.**

### 처리 흐름 (`process_new_emails`)
1. Gmail 검색: `from:automated@airbnb.com subject:지급`, 최근 **50건**
2. 각 메일마다 `is_email_processed(msg_id)` → **Notion API를 1건씩 조회**해 중복 확인
3. 새 메일이면 본문 받아 정규식 파싱
4. 파싱된 항목 중 **금액이 양수인 것만** Notion에 행 추가

---

## 3. 비용

- **Railway: 월 $5 결제됨** (2026-09-09 확인). 이전에는 트라이얼 크레딧으로 운영했고,
  2026-08-02에 크레딧이 소진되며 서비스가 정지된 이력이 있다(6장 참조).
- GitHub Actions: 퍼블릭 저장소 기준 무료.
- Gmail API / Notion API: 이 사용량에서는 무료.

$5는 **웹서버를 24시간 상시 가동**하는 비용이다. 실제로 일을 하는 시간은
하루 5회 × 20초 = **약 100초**다. 가동률로 보면 0.1% 수준이다.
여기에 더해, 아무 일도 하지 않는 구 크론 컨테이너(4장)도 같은 요금제 안에서 자원을 점유하고 있다.

---

## 4. 지금 발견된 문제 (2026-09-09 조사)

### 4-1. 구 Railway 크론이 "걸린" 상태로 남아 있었다 → **2026-09-09 해결됨**
`noble-inspiration` / `email-check-cron-v2`가 `cronSchedule: "0 * * * *"`로 여전히 설정돼 있었고,
2026-09-08T14:32Z 재배포 이후 인스턴스가 `status: CREATED` / `deploymentStopped: false`로 멈춰 있었다.

이는 2026-07-05, 2026-09-08 두 차례 장애와 **완전히 동일한 증상**이다(즉 같은 문제가 세 번째 재발).
Railway는 이전 실행이 끝나지 않으면 다음 스케줄을 조용히 건너뛴다.

실제로 웹서버 HTTP 로그(최근 24시간)에 도착한 호출은 **GitHub Actions 5건이 전부**였고,
매시 정각에 와야 할 Railway 크론 호출은 **단 한 건도 없었다**. 즉 이 서비스는
일은 하지 않으면서 컨테이너만 점유해 과금되고 있었다.

**조치(2026-09-09)**: 대시보드에서 Cron Schedule 해제 + 배포 제거.
`activeDeployments`가 비었음을 확인했다. 서비스 껍데기는 롤백 경로로 남겨뒀다.
(스케줄 변경·배포 제거는 CLI/GraphQL로 불가능해 대시보드 수동 작업이어야 한다.)

**남는 교훈**: 이 항목은 "플랫폼 크론이 조용히 멈춘다"는 문제가 **형태를 바꿔가며 세 번 반복**됐음을
보여준다. Function 타입 → curl 이미지로 바꿔도, 크론을 GitHub Actions로 옮긴 뒤에도
잔존 서비스에서 같은 증상이 재발했다. 5장의 논점(구조를 바꿀 것인가)의 핵심 근거다.

### 4-2. GitHub Actions 크론이 설정대로 실행되지 않는다
`cron: "37 * * * *"` (매시 37분)로 설정했으나, 최근 20시간 실제 실행은 **5회**뿐이다.

| 실행 시각(UTC) | 간격 |
|---|---|
| 09-09 10:04 | 4시간 53분 |
| 09-09 05:11 | 4시간 39분 |
| 09-09 00:32 | 2시간 32분 |
| 09-08 22:00 | 2시간 27분 |
| 09-08 19:33 | — |

분 단위도 37분이 전혀 지켜지지 않는다. GitHub Actions의 `schedule`은 보장이 아니라
best-effort이며, 러너가 혼잡하면 지연되고 다음 주기까지 밀리면 그 실행은 폐기된다.
**약 80%가 누락되고 있다.**

단, 이 시스템의 요구사항(1박 단위, 지연 무방)에서는 **실질적 문제가 아니다.**
조회 상한이 50건이라 밀린 메일은 다음 실행에서 회수된다.
문제는 "의도한 스케줄과 실제 동작이 다르다"는 점이며, 이 때문에
**정상인지 고장인지 로그만 봐서 구분할 수 없다.**

### 4-3. 60일 무활동 시 GitHub Actions 스케줄 자동 비활성화
GitHub은 저장소에 60일간 활동이 없으면 `schedule` 트리거를 자동으로 끈다.
이 저장소의 커밋은 7/3(13건), 7/19(3건), 8/9(1건), 9/8(2건), 9/9(1건)으로
**두 달 이상 공백이 생기기 쉬운 패턴**이다. 알림 메일은 오지만 놓치기 쉽다.

### 4-4. 중복 확인이 비효율적이다 — 실행당 20초
매 실행마다 메일 50건 각각에 대해 Notion API를 **개별 호출**한다(`is_email_processed`).
로그상 새 메일이 없어도 `[SKIP] ... 이미 처리됨`이 50줄 찍히고, 응답에 **20초**가 걸린다.
처리 상태를 로컬에 캐시하는 코드(`_load_processed`/`_save_processed`)가 있으나
**실제로는 호출되지 않는 죽은 코드**다. 컨테이너 파일시스템이 휘발성이라 무의미하기도 하다.

### 4-5. 파싱이 정규식 기반이라 게스트명 특수문자에 반복적으로 깨진다
같은 유형의 버그가 **네 차례** 발생했다.

| 시점 | 깨진 입력 | 조치 |
|---|---|---|
| 2026-07-19 | `Suah Kim,` (쉼표) | 정규식에 `,` 추가 |
| 2026-07-19 | `Ayuner 彭` (한자) | 정규식에 `一-鿿` 추가 |
| 2026-08-08 | `Tzu-Hao Yeh` (하이픈) | 정규식에 `-` 추가 |
| — | 아포스트로피 대비 | `'` 추가 |

현재 정규식:
```python
r"^([가-힣a-zA-Z一-鿿\s,'-]+?)\s{2,}(-?)[₩]?([\d,]+)\s*KRW$"
```
**허용 문자를 열거하는 방식(allowlist)이라 새로운 문자가 나올 때마다 깨진다.**
일본어 가나, 키릴, 태국어, 악센트 붙은 라틴 문자(`José`) 등은 여전히 실패한다.

더 나쁜 점은 **실패가 조용하다**는 것이다. 총액과 날짜는 파싱되므로 예외가 나지 않고,
`items`만 0건이 되어 아무것도 기록되지 않은 채 "처리됨"으로 넘어간다.
로그에도 `[OK] ... 0건 기록 완료`로 찍혀 성공처럼 보인다.

### 4-6. 감지 수단이 사실상 없다
자동 감지는 **GitHub Actions 실패 시 오는 이메일 하나뿐**이다. 그런데 이 시스템의
실제 장애는 대부분 **HTTP 200을 반환하면서 조용히 일을 안 하는** 형태였다.
과거 5차례 장애 중 4차례를 사용자가 "노션에 기록이 안 보인다"고 **직접 눈치채서** 발견했고,
2026-08-06~09-08 장애는 **한 달간 방치**됐다.

빠져 있는 것: 마지막 성공 기록 시각 감시, 입금 문자와 장부의 대사(reconciliation),
파싱 0건 경고.

---

## 5. 취약점 요약 (설계 관점)

| # | 문제 | 성격 |
|---|---|---|
| 1 | 상시 서버가 필요 없는데 상시 서버를 쓴다 ($5/월, 가동률 0.1%) | 비용/구조 |
| 2 | 크론이 외부 서비스에 의존하고, 두 번 다 조용히 멈췄다 | 신뢰성 |
| 3 | 트리거와 로직이 별도 플랫폼(GitHub↔Railway)에 분리돼 장애 지점이 2배 | 구조 |
| 4 | 정규식 allowlist 파싱 — 새 문자마다 깨지고 실패가 조용함 | 견고성 |
| 5 | Notion을 DB로 쓰면서 중복 확인을 건당 API 호출로 함 | 효율 |
| 6 | 실패 감지가 사용자 육안 확인에 의존 | 운영 |
| 7 | 시크릿이 하드코딩된 고정 문자열이고 저장소 메모에 평문 노출 | 보안(경미) |

**5차례 장애의 원인 분포**: 인프라/플랫폼 문제 3건(크론 걸림 ×2, 크레딧 소진 ×1),
인증 만료 1건, 파싱 버그 4건(별도 집계). 즉 **문제의 절반 이상이
"코드가 아니라 이 코드를 굴리는 방식"에서 나왔다.**

---

## 6. 장애 이력 전체

| 날짜 | 증상 | 근본 원인 | 발견 경로 |
|---|---|---|---|
| 2026-07-05 | 기록 안 됨 | Railway "Function" 타입 크론이 HTTP 서버를 띄운 채 종료되지 않아 매 사이클 스킵 | 사용자 |
| 2026-07-19 | 7/8 이후 기록 안 됨 | Gmail OAuth refresh token 만료. GCP 동의화면이 "테스트중"이라 7일마다 만료 | 사용자 |
| 2026-07-19 | 특정 1건 누락 | 게스트명 쉼표 파싱 실패 | 사용자 |
| 2026-08-02 | 8/2 이후 기록 안 됨 | Railway 트라이얼 크레딧 소진 → 컨테이너 정지. `restartPolicy: ON_FAILURE`라 정상종료(exit 0)엔 재시작 안 걸림 | 사용자 |
| 2026-08-08 | 8/6 1건 누락 | 게스트명 하이픈 파싱 실패 | 사용자 |
| 2026-09-08 | **한 달간 전부 누락** | Railway 크론 인스턴스 걸림(7/5 재발). 게다가 조회 상한이 10건이라 밀린 메일 6건이 조회창 밖으로 밀려나 웹훅 재호출로도 복구 불가 | 사용자 |

### 이력에서 얻은 교훈 (재검증 시 참고)
- **`nextCronRunAt` 갱신은 실행 성공의 증거가 아니다.** 스케줄 정의로만 계산되는 값이라
  크론이 멈춰 있어도 정상으로 보인다. 실제 확인은 **호출받는 쪽의 HTTP 로그**로만 가능하다.
- **웹훅 응답의 `processed`는 "Notion에 기록된 건수"가 아니라 "훑어본 메일 수"다.**
  "10건 처리됨"에 속아 실제 누락을 놓칠 뻔한 적이 있다.
- 실제 반영 확인은 **Gmail 전수 ↔ Notion 대조**로만 해야 한다.

### 장부 대상이 아닌 정상 케이스 (오탐 방지)
Gmail 쿼리에는 걸리지만 기록되지 않는 게 정상인 것들:
- `19f69b0300249a35` (7/16) 호스트가 게스트에게 지급한 건
- `19f407642ac4a379` (7/8) 초안 저장 알림
- 항목이 전부 음수인 정산 메일(공동 호스트 차감 등) — 단 실입금은 있으므로 수동 기록 필요

---

## 7. 현재 상태 판정 (2026-09-09 실측)

- 웹서버: 정상. 헬스체크 200, 최근 호출 모두 HTTP 200.
- 데이터: **누락 없음.** 최근 실행에서 모든 메일이 `이미 처리됨`으로 스킵됨.
- GitHub Actions: 동작하나 스케줄대로는 아님(4-2).
- Railway 구 크론: **정리 완료.** 스케줄 해제 + 배포 제거 확인(4-1).

즉 **지금 당장 데이터가 새고 있지는 않다.** 이 문서의 논점은 장애 대응이 아니라
"이 구조를 유지할 가치가 있는가"이다.

---

## 8. 검증 시 물어볼 만한 질문

다른 곳에서 대안을 검토한다면 아래를 짚어보면 유용하다.

1. **하루 5회, 회당 20초 일하는 작업에 상시 서버가 필요한가?**
   서버리스/스케줄 실행 방식(Cloudflare Workers Cron, GCP Cloud Run Jobs + Scheduler,
   AWS Lambda + EventBridge 등)으로 옮기면 무료 티어 안에 들어올 여지가 있다.
   단, Gmail/Notion 토큰 보관과 Python 의존성(google-api-python-client)이 제약이 된다.

2. **트리거와 로직을 한 플랫폼으로 합칠 수 있는가?**
   현재는 GitHub Actions가 Railway를 깨우는 2단 구조라 장애 지점이 둘이다.
   GitHub Actions 안에서 Python을 직접 실행하면(웹서버 자체를 없애면) Railway가 통째로 불필요해진다.
   → **이 방향이 비용·구조 양쪽에서 가장 크게 줄어드는 안으로 보인다.**
   확인할 점: Actions에 시크릿 보관(가능), 실행 시간(20초 → 캐시 없이도 무료 한도 내), 스케줄 신뢰성(4-2 그대로 남음).

3. **정규식 파싱을 대체할 수 있는가?**
   메일 본문은 에어비앤비가 임의로 바꿀 수 있는 형식이다. HTML 파트 구조 파싱,
   또는 LLM 기반 추출로 바꾸면 게스트명 문자 문제는 사라진다.
   더 근본적으로는 **에어비앤비 공식 API나 CSV 정산 내역 다운로드**로 대체 가능한지 검토할 가치가 있다.
   메일 파싱은 본질적으로 우회책이다.

4. **파싱 실패를 시끄럽게 만들 수 있는가?**
   `items == 0`인데 총액이 파싱된 경우는 거의 확실히 버그다. 지금은 조용히 넘어간다.
   여기서 예외를 던지거나 알림을 보내면 4번의 반복 장애 중 3번은 즉시 발견됐을 것이다.

5. **감시를 어떻게 붙일 것인가?**
   "마지막 성공 기록이 N시간 이상 없으면 알림"이 이 시스템에 가장 필요한 한 가지다.
   Healthchecks.io 같은 dead-man's-switch 무료 서비스로 붙일 수 있다.

---

## 9. 참고 명령어 (진단용)

```bash
# GitHub Actions 실행 이력
gh run list --repo alwayswin2/sns --workflow check-emails.yml

# 실제 웹훅 도착 여부 — 크론 동작 확인의 유일한 확실한 방법
railway link --project splendid-warmth
railway logs --service sns --http --since 24h | grep check-emails

# 애플리케이션 로그 (파싱 결과)
railway logs --service sns --since 24h

# 크론 서비스 상태 (cronSchedule, 인스턴스 status 확인)
railway link --project noble-inspiration
railway status --json

# 수동 트리거
curl -X POST https://sns-production-7b5b.up.railway.app/webhook/check-emails \
  -H "x-secret: <WEBHOOK_SECRET>"
```

Windows 콘솔에서 `₩` 등 유니코드 출력 시 cp949 인코딩 에러가 나므로
`PYTHONIOENCODING=utf-8`을 지정하거나 파일로 출력할 것.
