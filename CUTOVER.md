# 전환 계획 (기존 시스템 → Apps Script)

> 작성일: 2026-09-10 · **갱신: 2026-09-15**
>
> ⚠️ **작성 당시의 "아무것도 실행되지 않았다"는 서술은 더 이상 사실이 아닙니다.**
> 2026-09-15 확인 결과 **Apps Script 는 이미 배포되어 4시간 트리거로 가동 중**입니다.
> (0단계·4단계가 사람 손으로 완료된 상태이며, 그 사실이 이 문서에 반영되지 않았었습니다.)
>
> 아래 단계 중 **0~5단계는 완료**, **6단계부터가 남은 작업**입니다.
> 각 단계의 실제 진행 상태는 이 문서가 아니라 **실제 신호**(알림 메일 / Notion 장부 / 트리거 목록)로
> 확인하세요 — 문서가 낡아 잘못된 판단을 유발한 전례가 이미 있습니다.

---

## 지금 상태

*(2026-09-15 실측 기준)*

| 구성요소 | 상태 |
|---|---|
| Railway `sns` (FastAPI 웹서버) | **그대로 가동 중** — 아직 안 껐음 |
| GitHub Actions `check-emails.yml` | **그대로 동작 중** (`37 * * * *`) — 6단계에서 끌 대상 |
| Railway `email-check-cron-v2` | 2026-09-09에 사용자가 이미 중단(껍데기만 존재) |
| Notion 정산 장부 | 53행. **GAS 가 쓴 행은 0행**, 전부 구 시스템 기록 |
| 신규 Apps Script | **배포·가동 중** — `processNewPayoutEmails` 4시간 주기 |

**두 시스템이 동시에 가동 중이며, 이것은 4단계가 의도한 정상 상태입니다.**
중복은 이메일ID·항목키로 막히며, 실제로 **48건에서 48번 모두 올바르게 억제**되었습니다
(GAS 가 구 시스템의 기존 행을 찾아 `SKIPPED_DUPLICATE` 처리 → 쓰지 않음).

> 다만 **GAS 가 이 DB 에 행을 직접 만들어 본 적은 아직 없습니다.**
> 중복 방지는 검증됐지만 **쓰기 경로는 미검증**이며, 이것이 6단계 전에 확인할 유일한 항목입니다.

---

## 전환 순서

### 0단계 — 병행 준비 (변경 없음)

`gas/README_DEPLOY.md` 를 따라 Apps Script 프로젝트를 만듭니다.
**아직 `setupTriggers` 는 실행하지 마세요.** 트리거 없이 수동 실행만 합니다.

- [ ] Apps Script 프로젝트 생성, 파일 9개 붙여넣기
- [ ] Script Properties 입력 (`NOTION_TOKEN`, `NOTION_DATABASE_ID`, `ALERT_EMAIL`)
- [ ] `testConnection` 실행 → Gmail·Notion 둘 다 `정상` 확인
- [ ] `dryRunRecent` 실행 → `PARSE_ERROR` 가 0건인지 확인

> 이 시점까지 Notion 에는 아무것도 쓰이지 않습니다.

---

### 1단계 — 금액 정책 (결정 완료: A안)

**결정: `입금액` = 메일 지급 총액(= 그 건으로 실제 통장에 들어온 돈) 유지. 과거 행 보정 없음.**

정산 메일 한 건의 구조는 보통 이렇습니다:
```
숙소              +167,559   ← 숙박비 몫
공동 호스트 분배    −8,377    ← 공동 호스트에게 나가는 몫
─────────────────────────
지급 총액          159,182   ← 실제 통장에 들어온 돈
```
칸 이름이 `입금액` 이므로 **159,182 가 의미에 맞고**, 통장 대사도 이 값으로 됩니다.
기존 장부 46행이 이미 이 방식이므로 **과거와 일관되며 보정이 필요 없습니다.**

- 신규 시스템 기본값이 이 정책입니다(`AMOUNT_FIELD_MODE` 를 설정하지 않으면 `total`).
- 별도로 할 일 없음. 이 단계는 확인만 하고 넘어가면 됩니다.

**단 하나의 예외 — 한 메일에 정산 대상 게스트가 2명 이상인 경우**

이때는 총액을 두 행에 각각 넣으면 합계가 부풀려집니다.
실제로 이런 메일은 전체 48건 중 **1건**이었습니다.

`19f0215a1fb4e56c` (2026-06-26, 총액 64,618)
| 게스트 | 현재 장부 | 실제 그 예약 금액 |
|---|---|---|
| 정필 서 | 64,618 | 64,617 |
| 정수 김 | 64,618 | 68,019 |
→ 두 행 합계 129,236 이지만 실제 입금은 64,618 한 번뿐입니다.

신규 시스템은 이런 메일을 **기록하지 않고 `MANUAL_REVIEW` 알림**을 보냅니다.
사람이 배분을 보고 직접 정리하도록 하기 위함입니다.

- [ ] (선택) 위 2026-06-26 두 행을 어떻게 정리할지 결정
      — 한 행을 0 으로 두거나, 비고에 "총액 중복" 메모를 남기는 방법 등.
      건드리지 않아도 앞으로의 기록에는 영향이 없습니다.

---

### 2단계 — 병행 검증 (기존 시스템 계속 가동)

기존 시스템이 계속 도는 상태에서, Apps Script 를 **수동으로만** 실행합니다.

- [ ] `processNewPayoutEmails` 수동 1회 실행
- [ ] 로그의 `inserted_rows` 확인 — 기대값은 **0** 이어야 합니다
      (기존 시스템이 이미 다 기록해 두었으므로 신규 기록이 없어야 정상)
- [ ] Notion 에 중복 행이 생기지 않았는지 눈으로 확인
- [ ] `manual_review=3` 은 정상입니다 (아래 3건). 알림 메일이 옵니다.

*(장부 실제 기록값은 2026-09-15 전수 감사로 확인함)*

| 메일ID | 날짜 | 사유 | 장부에 실제로 기록된 값 |
|---|---|---|---|
| `19ffe89304377d2b` | 2026-08-14 | 전 항목 음수 | 미영 황 **283,374** (사용자 수동 기록) |
| `19f0c51fa65cc094` | 2026-06-28 | 항목합≠총액, USD 크레딧 섞임 | Amy Min **170,793** + 별도 "기타크레딧" **84,494** → 합계 255,287 |
| `19f0215a1fb4e56c` | 2026-06-26 | 게스트 2명 | 정수 김 **64,618** / 정필 서 **64,618** (2행) |

> **정정**: 이전 판에는 `19f0c51fa65cc094` 가 255,287 한 행으로 기록된 것처럼 적혀 있었으나,
> 실제로는 항목합(170,793)과 차액(84,494)이 **두 행으로 나뉘어** 있습니다.
> 합치면 255,287 이므로 A안과 모순되지 않으며, 추가 조치가 필요 없다는 결론은 그대로입니다.

> 이 3건은 **이미 장부에 반영돼 있으므로 추가 조치가 필요 없습니다.**
>
> **✅ 2026-09-15 조치 완료** — 아래 함수를 편집기에서 1회 실행했고,
> 로그가 `51건이 처리 완료로 표시됨`(기존 48 + 3)이었습니다. **알림은 이것으로 멈춥니다.**
> ```javascript
> function silenceKnownManualReviews() {
>   ['19ffe89304377d2b', '19f0c51fa65cc094', '19f0215a1fb4e56c'].forEach(markProcessed);
>   Logger.log('완료: ' + readProcessedIds_().length + '건이 처리 완료로 표시됨');
> }
> ```

**알림이 8시간마다 반복됐던 이유** (같은 증상 재발 시 참고)

두 상수가 어긋난 결과입니다 — 버그가 아닙니다.
- `Config.gs` `TRIGGER_INTERVAL_HOURS = 4` (처리 4시간마다)
- `Config.gs` `ALERT_DEDUPE_MINUTES = 360` (알림 억제 6시간)

→ T 알림 / T+4h 억제(창 안) / T+8h 재알림 → **8시간 주기**.
무한 반복된 근본 이유는 `Main.gs:81-90` 에서 **MANUAL_REVIEW 가 의도적으로 `markProcessed()` 를
호출하지 않기** 때문입니다("사람이 확인해야 하므로"). 사람이 손대기 전까지 매 실행 재알림됩니다.
**장부는 안전합니다** — `Main.gs:156-160` 이 Notion 쓰기 **전에** 반환하므로 행은 생기지 않습니다.

> ⚠️ **이 억제는 영구적이지 않습니다.** `PROCESSED_IDS` 는 Script Properties 캐시일 뿐이며
> (`ProcessingState.gs:4-6`), `clearProcessedCache()` 실행이나 속성 초기화 시 알림이 재개됩니다.

**2026-06-26 건(64,618 × 2행)은 보정하지 않고 유지하기로 결정했습니다 (2026-09-15).**
그 날짜의 장부 합계는 129,236, 실제 입금은 64,618 로 **차이가 있음을 알면서 남겨둔 것**입니다.
나중에 다시 발견하더라도 버그로 취급해 고치지 마세요. 재론하려면 사용자 확인이 먼저입니다.
- [ ] 며칠간 새 정산 메일이 오면, 기존 시스템이 기록한 뒤
      Apps Script 를 수동 실행해 **중복이 안 생기는지** 확인

> 이 검증이 통과하면 중복 방지가 실제 데이터에서 작동함이 확인된 것입니다.

---

### 3단계 — 과거 데이터 보정 (A안 채택으로 **불필요**)

`입금액` 의 의미를 그대로 유지하므로 **과거 46행을 고칠 일이 없습니다.**

참고로 손대지 말아야 할 행이 4개 있습니다(전부 사용자가 직접 넣은 것):

| 정산일 | 내용 | 비고 |
|---|---|---|
| — | 빈 행 | |
| 2026-06-28 | 기타크레딧 84,494 | "추천인" — Amy Min 건의 USD 크레딧 차액을 수동 기록 |
| 2026-07-16 | 세미 박 −78,803 | "온수관터져서 환불" |
| 2026-08-14 | 미영 황 283,374 | 전 항목 음수 건을 총액으로 수동 기록 |

> 2026-06-28 건은 신규 시스템이 `MANUAL_REVIEW`(항목합 170,793 ≠ 총액 255,287)로
> 잡아내는 바로 그 메일이고, 차액이 정확히 84,494 입니다.
> 이미 사람이 손으로 메워둔 것이라 신규 검증 로직이 실제 운영 판단과 일치함을 보여줍니다.

---

### 4단계 — 신규 시스템 자동 실행 시작

- [ ] Apps Script 에서 `setupTriggers` 실행
- [ ] 좌측 **트리거** 메뉴에서 3개 등록 확인
      (`processNewPayoutEmails` 4시간 / `dailyHealthCheck` 09시 / `reconcileNightly` 04시)

이제 **두 시스템이 동시에 돕니다.** 중복은 메일ID·항목키로 막히므로 안전합니다.

---

### 5단계 — 병행 관찰 (권장 1~2주)

- [ ] 새 정산 메일이 최소 3~5건 들어오는 기간 동안 관찰
- [ ] 매번 Notion 에 **한 번만** 기록되는지 확인
- [ ] `getSystemStatus` 로 `lastSuccessfulRun` 이 계속 갱신되는지 확인
- [ ] 오류 알림 메일이 오지 않는지 확인

> 여기서 문제가 없으면 기존 시스템을 꺼도 됩니다.

---

### 6단계 — 기존 크론 중지 (되돌리기 쉬운 것부터)

- [ ] GitHub Actions 중지
      `.github/workflows/check-emails.yml` 에서 `schedule:` 블록만 주석 처리하고 커밋
      (`workflow_dispatch` 는 남겨두면 수동 실행으로 언제든 되돌릴 수 있습니다)

- [ ] 3~7일 관찰 — Apps Script 만으로 정상 기록되는지 확인

> 이 단계는 파일 한 줄 주석으로 즉시 되돌릴 수 있습니다.

---

### 7단계 — Railway 웹서버 중지

- [ ] Railway 대시보드 → `splendid-warmth` 프로젝트 → `sns` 서비스
- [ ] **Settings → Remove Deployment** (서비스 삭제가 아니라 배포 제거)
- [ ] `curl https://sns-production-7b5b.up.railway.app/` 이 응답하지 않는지 확인

> 이 시점부터 **월 $5 과금이 멈춥니다.**
> 서비스 껍데기는 남겨두면 필요할 때 재배포로 되돌릴 수 있습니다.

- [ ] MacroDroid 를 쓴다면, 그 전에 반드시 웹훅 URL 을 Apps Script 웹 앱 주소로 바꿔둘 것
      (안 바꾸면 입금 문자 트리거가 끊깁니다 — 단, 4시간 주기 실행은 계속 되므로 기록 자체는 유지됩니다)

---

### 8단계 — 최종 대사

- [ ] Apps Script 에서 `reconcileRecentPayouts` 실행
- [ ] 로그에서 `누락 0건` 확인
- [ ] Notion 장부를 직접 열어 최근 정산이 다 들어와 있는지 눈으로 확인

---

### 9단계 — (선택) 완전 정리

위 모든 단계가 문제없이 2~4주 유지된 뒤에만 하세요.

- [ ] Railway 프로젝트 `splendid-warmth` 삭제
- [ ] Railway 프로젝트 `noble-inspiration` 삭제
- [ ] GCP 프로젝트 `auto1-501216` 의 Gmail OAuth 클라이언트 정리
      (Apps Script 는 이 자격증명을 쓰지 않습니다)
- [ ] 저장소에서 `src/`, `Procfile`, `railway.toml`, `requirements.txt` 제거
      (기록 보존을 위해 삭제 대신 `legacy/` 로 옮기는 것을 권장)

> `config/.env`, `config/gmail_token.json` 은 `.gitignore` 에 있어 저장소에는 없지만,
> 로컬 파일은 남아 있습니다. Railway 삭제 후 함께 정리하세요.

---

## 되돌리는 방법

각 단계별로 원상복구가 가능합니다.

| 되돌릴 것 | 방법 |
|---|---|
| Apps Script 자동 실행 중지 | 좌측 **트리거** 메뉴에서 3개 삭제 |
| GitHub Actions 재가동 | `check-emails.yml` 의 `schedule:` 주석 해제 후 push |
| Railway 웹서버 재가동 | Railway 대시보드에서 **Redeploy** |
| SMS 웹훅 되돌리기 | MacroDroid URL 을 예전 Railway 주소로 복원 |

**9단계(삭제)만 되돌릴 수 없습니다.** 그래서 맨 마지막에, 충분히 기다린 뒤에 하도록 배치했습니다.

---

## 전환 후 남는 것

```
에어비앤비 정산 메일 → Gmail → Apps Script(4시간마다) → Notion 정산 장부
                                      ↑
              입금 문자 → MacroDroid → doPost()
```

| 항목 | 전환 전 | 전환 후 |
|---|---|---|
| 월 비용 | $5 | **0원** |
| 외부 구성요소 | Railway + GitHub Actions + FastAPI | **없음** |
| Gmail 토큰 관리 | refresh token 수동 갱신 필요 | **불필요** |
| 장애 감지 | 사용자 육안 (5회 중 4회) | 자동 알림 + 일일 점검 + 야간 대사 |
